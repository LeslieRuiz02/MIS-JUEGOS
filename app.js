document.addEventListener('DOMContentLoaded', () => {
    const miBoton = document.getElementById('miBoton');

    miBoton.addEventListener('click', () => {
        alert('¡Bienvenido! Has hecho clic en el botón. Ya estamos listos para practicar Git.');
    });
});
